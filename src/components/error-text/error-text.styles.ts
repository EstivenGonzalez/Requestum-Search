import styled from 'styled-components/native'

import { Topography } from '@components/topography'

export const StyledErrorText = {
  Retry: styled(Topography)`
    text-decoration-line: underline;
  `,
}
